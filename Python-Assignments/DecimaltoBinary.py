# We add any two number (which represents decimal) then return it as binary
# Decimal (two added number) >>> Binary
def add_binary(a,b):
  while a+b >= 0:
    decimal = a+b
    remainder = [] #Use the array to convert it to binary (adding all remainder equal binary)
    #binary = 
    break

    
add_binary(120,1)
